using App.Views;
using Firebase.Auth;
using Firebase.Auth.Repository;

namespace App
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
        }
    }
}